# Simple Bundler

A lightweight Shopify public app for creating simple product bundles for new and growing stores.

## Local development
1. Install dependencies: `npm install`
2. Start the dev server: `shopify app dev`

> Copy `.env.example` to `.env` and fill in real values. Never commit `.env`.
